{
  "name": "haunthub",
  "version": "1.0.0",
  "description": "A spooky AI assistant with Victorian ghost butler persona",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm start",
    "dev:mcp": "echo 'MCP tools ready - configure in Kiro settings'",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "setup": "npm run install:all && npm run setup:mcp",
    "setup:mcp": "node setup-mcp.js",
    "setup:gemini": "cd backend && npm install && echo 'Backend ready! Add your GEMINI_API_KEY to backend/.env'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "ai",
    "assistant", 
    "halloween",
    "ghost",
    "mcp",
    "kiro",
    "hackathon"
  ],
  "author": "HauntHub Team",
  "license": "MIT"
}